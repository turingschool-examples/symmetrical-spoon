<h1>Competitions</h1><br>
<table style='width: 50%; border-color: white;'>
  <tr>
    <th>Competition Name</th>
  </tr>
    <% @competitions.each do |competition| %>
      <tr>
        <td><%= link_to "#{competition.name}", "/competitions/#{competition.id}", method: :get %></td>
      </tr>
    <% end %>
</table><br>